module.exports = {
  runtimeCompiler: true,
  devServer: {
    // open: process.platform === 'darwin',
    // host: 'localhost',
    port: 4000,
    // https: false,
    // hotOnly: false,
    // proxy: null // 设置代理
   },
}